name: M6-ROADMAP-001C immutable ImprovementSpec
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  roadmap-001c:
    if: github.head_ref == 'experiment/m6-roadmap-001c-immutable-spec'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      OLLAMA_HOST: 127.0.0.1:11440
      OLLAMA_NO_CLOUD: "1"
      OLLAMA_MODEL: qwen2.5-coder:7b
      BASELINE_SHA: 260b5f9e20bf70a6b9ca087bc91e22a009ed77b9
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install research harness
        run: python -m pip install -e '.[factory]'
      - name: Materialize exact production source
        run: |
          git worktree add --detach /tmp/residual-source "$BASELINE_SHA"
          test "$(git -C /tmp/residual-source rev-parse HEAD)" = "$BASELINE_SHA"
          test -z "$(git -C /tmp/residual-source status --porcelain)"
      - name: Validate repair diagnostics
        run: |
          python -m unittest \
            tests.station.test_station.StationTests.test_repair_detail_retains_terminal_root_cause_within_bound \
            tests.station.test_station.StationTests.test_repeated_failed_patch_is_explicit_repair_finding \
            tests.station.test_station.StationTests.test_repair_attempt_receives_previous_candidate_without_mutating_fresh_baseline \
            tests.station.test_station.StationTests.test_batch_can_repair_on_fourth_attempt_without_weakening_checks -v
      - name: Install Ollama
        run: curl -fsSL https://ollama.com/install.sh | sh
      - name: Start dedicated Ollama endpoint
        run: |
          nohup ollama serve > /tmp/ollama-roadmap-001c.log 2>&1 &
          echo $! > /tmp/ollama-roadmap-001c.pid
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:11440/api/tags >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/ollama-roadmap-001c.log
          exit 1
      - name: Pull implementation model
        run: ollama pull "$OLLAMA_MODEL"
      - name: Record runtime identity
        run: |
          ollama --version | tee /tmp/ollama-version-roadmap-001c.txt
          ollama list | tee /tmp/ollama-models-roadmap-001c.txt
          curl -fsS http://127.0.0.1:11440/api/tags > /tmp/ollama-tags-roadmap-001c.json
      - name: Execute authoritative immutable-contract trial
        run: python scripts/m6_roadmap_001c_immutable_spec.py --source /tmp/residual-source --model "$OLLAMA_MODEL" --base-url http://127.0.0.1:11440 --output runs/m6-roadmap-001c/evidence.json
      - name: Retain evidence
        if: always()
        run: |
          test ! -f runs/m6-roadmap-001c/evidence.json || cat runs/m6-roadmap-001c/evidence.json
          tail -300 /tmp/ollama-roadmap-001c.log || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: m6-roadmap-001c-immutable-spec-evidence
          path: |
            runs/m6-roadmap-001c
            /tmp/ollama-roadmap-001c.log
            /tmp/ollama-roadmap-001c.pid
            /tmp/ollama-version-roadmap-001c.txt
            /tmp/ollama-models-roadmap-001c.txt
            /tmp/ollama-tags-roadmap-001c.json
