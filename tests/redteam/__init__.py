"""Adversarial regression corpus (track K).

Requirement IDs (track K):
  K-R1: Every attack in this corpus MUST be asserted blocked or observed.
  K-R2: Where the receipt system supports it, a FAIL receipt MUST be issued
        recording the attempt (value_hash binds the attack description).
  K-R3: These are security regression tests, not real exploits: payloads
        MUST be inert and MUST NOT target anything outside the test sandbox.
"""
