Continue SNYK-R4-02: adversarially audit RESIDUAL archive extraction, preserving protection-layer attribution.

You are running on local gemma-4-e4b-abliterated through Codex. Work only in /tmp/residual-r4-gemma-lab. Original repository /mnt/c/Users/jinis/residual-snyk-r4 is READ ONLY for this task. DO NOT edit original repository, R4-01, product code, configs, or run git commits. No external inference or network. Use only /tmp/residual-r4-venv/bin/python, verified Python 3.12.13. Set PYTHONDONTWRITEBYTECODE=1. All archive escape targets MUST remain within a newly allocated tempfile directory under /tmp; never use actual system paths. Existing tests: /mnt/c/Users/jinis/residual-snyk-r4/tests/security/test_r4_02_archive_authority.py. Actual extractor: residual.station.models.Ollama.install in /mnt/c/Users/jinis/residual-snyk-r4/residual/station/models.py. Import original module via PYTHONPATH; do not replace its extraction implementation. You may mock download, manifest asset/hash, platform selector, binary/start so no install service is launched. The fixture digest must match for archive tests. Clearly label this as an attacker-controlled archive abstraction; production manifest digest remains a trust boundary.

Read the extraction code and existing test first. Then create and RUN a focused test runner in this lab using the real extractor unchanged and intentionally unguarded negative control over identical fixtures (use tarfile.extractall(filter='fully_trusted') as control). Need working benign positive control. Cover ../, absolute targets within tempfile, nested traversal, symlink alone, symlink-then-file, hardlink to external-to-dest tempfile sentinel, separator/normalization variants, duplicate member overwrite. Include TAR, ZIP, TAR.ZST where supported, or explicitly mark untested. Existing zstd and tar available. Test both observable outside writes and created link authority, not merely exceptions. Report actual evidence, distinguish archive-provided links and preexisting links. Check requires-python in pyproject.toml and state runtime dependency; no unsupported claim that tar safety is RESIDUAL validation or that Python 3.12 is the first patched version. Do NOT fix product code.

Use terminal tools to run tests; do not just promise execution. For normal exec_command omit justification. Keep commands short and outputs bounded. Write a JSON or Markdown report in this lab with per-case actual result and protection layer: RESIDUAL explicit validation, Python tarfile data filter, filesystem/runtime behavior (including GNU tar or zipfile if applicable), or no protection. Preserve test source for reviewer. Return exact commands, tested runtime, controls, real results, runtime assumptions, observed errors, and limitations. Never infer a test passed from an exception alone. Final fields:
RUNTIME_TESTED, BENIGN_CONTROL, UNGUARDED_NEGATIVE_CONTROL, REAL_EXTRACTOR_RESULT, TRAVERSAL, ABSOLUTE_PATH, SYMLINK_ESCAPE, HARDLINK_ESCAPE, NORMALIZATION_VARIANTS, PROTECTION_LAYER (RESIDUAL|PYTHON_RUNTIME|FILESYSTEM|NONE|MIXED), MINIMUM_SAFE_RUNTIME (version or UNKNOWN), DISPOSITION (EXPLOITABLE|CONSTRAINED|FALSE_POSITIVE|NEEDS_MORE_EVIDENCE), FIX_REQUIRED (YES|NO), LIMITATIONS.
