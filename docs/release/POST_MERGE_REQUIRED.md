# Post-Merge Requirement

Merging the hardening does not itself prove the production regression fixed. The first Pages deployment of the merged SHA must complete its published WebVM checks and published real Puter SDK-load checks. Only then may the corresponding layers be marked PASS.
