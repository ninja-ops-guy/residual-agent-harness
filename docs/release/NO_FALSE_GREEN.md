# No False Green Rule

A release is not "green" for a capability that was not directly exercised.

CI dashboards aggregate jobs; release claims must not. Preserve the per-layer state in human summaries and evidence artifacts. In particular, a green Pages job with provider fixture coverage is not real provider inference evidence, and a green narrow Chromium run is not physical iPhone/WebKit evidence.
