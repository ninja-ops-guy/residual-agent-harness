# Maintainer Acceptance — Demo Provider Hardening

Accept only when the hardening head has fresh applicable PR qualification and the underlying provider COI fix is present. After eventual merge to main, qualification is incomplete until the first Pages deployment passes published WebVM desktop+narrow checks and the new real Puter SDK-load checks.

A post-deploy SDK-load PASS means only that the real SDK initialized. It must not be recorded as real authentication or inference acceptance.
