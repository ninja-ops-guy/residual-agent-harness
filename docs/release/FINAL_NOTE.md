# Final Note

The intended steady state is simple: agents cannot call the demo/provider release qualified without seeing exact-SHA WebVM evidence and a separate real external SDK-load result. Stronger claims—auth, inference, physical WebKit—remain explicitly separate.
