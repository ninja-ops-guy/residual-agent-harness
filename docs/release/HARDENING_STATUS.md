# Hardening Candidate Status

Candidate lineage contains the narrow `/provider/` COI bypass from the provider-fix branch plus production real-SDK load acceptance, CI source guardrails, repository agent instructions, and release documentation.

This document does not declare the candidate merged or production-qualified. Fresh exact-head CI is required. After eventual merge, the first production Pages deployment and its real-SDK load evidence remain authoritative.
