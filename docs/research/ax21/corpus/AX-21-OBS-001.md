# AX-21-OBS-001 — Observer livelock under model-mediated tool execution

**Recorded:** 2026-09-24  
**Program:** AX-21  
**Status:** OBSERVED / ROOT CAUSE UNRESOLVED  
**Environment:** LEGION; Hammer OpenClaw; Kimi-linked inference/tool path  
**Classification:** observer livelock / repeated semantic tool intent without demonstrated epistemic progress

## Observation

During a scheduled RESIDUAL health-check sequence, Hammer repeatedly attempted to establish the existence or usability of:

`/root/.openclaw-forgev2/workspace-main/main/residual/.venv/bin/python`

Observed intents included `ls`, `ls -l`, `test -f`, attempts to `read` the executable, and compound commands intended to run `budget_fallback.py status` and `python -m residual demo`. Across the supplied transcript, no authoritative result for the actual health question was surfaced. The observer repeatedly returned to the already-addressed interpreter-existence proposition.

## Evidence boundary

This observation does **not** establish that Kimi, OpenClaw, the tool transport, LEGION, the Python environment, or RESIDUAL is individually the root cause. It establishes only that the Kimi-mediated OpenClaw observation path exhibited repeated semantically equivalent tool intent without demonstrated progress to durable health evidence.

Do not relabel this record as a RESIDUAL target failure without independent target evidence.

## Research significance

This incident motivates an explicit distinction between:

- `TARGET_FAILURE`: the monitored workload is unhealthy;
- `OBSERVER_FAILURE`: the monitoring/agent path cannot establish target state;
- `OBSERVER_STALLED`: the observer remains active but produces no material epistemic progress;
- `UNKNOWN`: evidence is insufficient to distinguish the above.

It also motivates shared communications as control-plane state: agents should be able to discover what propositions have already been established, by whom, with what evidence and freshness, before issuing semantically redundant tool calls.

## Candidate metrics

1. semantic-intent repetition count;
2. repeated-resource count;
3. wall-clock time without epistemic state transition;
4. tool-intent/result ratio;
5. durable-receipt production latency;
6. redundant inference/tool cost;
7. independent-observer disagreement;
8. recovery time after deterministic fallback;
9. cross-agent reuse rate of existing evidence;
10. tenant-boundary violations during evidence reuse.

## Candidate AX-21 hypothesis

**H-AX21-SC:** Swarms operating against durable shared epistemic state will exhibit fewer redundant tool actions, shorter recovery time, lower inference consumption, and fewer observer-livelock episodes than otherwise equivalent swarms whose coordination depends primarily on individual model context.

## Required follow-up

Preserve the raw source transcript outside this normalized record. Execute a deterministic local probe to establish target ground truth. Reproduce under controlled conditions before attributing causality to any model/provider/orchestrator component. Compare no-shared-state, shared-message-only, and shared-epistemic-state conditions with held-constant workload/model/tool environment where practical.
