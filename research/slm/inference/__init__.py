"""SLM inference runtime: backend abstraction, export, CPU serving.

All artifacts handled here are non-production test artifacts until the
SLM-00 benchmark freeze lifts (see research/slm/SECURITY-BOUNDARY.md).
"""
