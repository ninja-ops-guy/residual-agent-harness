{
  "plan_id": "plan-2026-09-14-demo",
  "plan_hash": "5c1f0a2b8d4e6f708192a3b4c5d6e7f8091a2b3c4d5e6f708192a3b4c5d6e7f80",
  "intent": "Add rate limiting, audit logging, and health endpoints to the reference service.",
  "serial_estimate_minutes": 96,
  "risk_flags": ["NET-2 touches shared middleware", "audit schema not pinned"],
  "clarifications": [
    {
      "id": "CLAR-1",
      "question": "Should rate limits be per-API-key or per-IP?",
      "status": "resolved",
      "answer": "Per-API-key with per-IP fallback."
    }
  ],
  "requirements": [
    {
      "id": "REQ-1",
      "title": "Rate-limit middleware",
      "description": "Token-bucket limiter keyed by API key, 100 req/min default.",
      "acceptance": ["429 after budget exhaustion", "Retry-After header present"],
      "dependencies": [],
      "provenance": {"intent_span": [12, 34]},
      "swarm": "swarm-alpha",
      "risk": "medium"
    },
    {
      "id": "REQ-2",
      "title": "Audit log schema",
      "description": "Append-only audit events with actor, action, target, timestamp.",
      "acceptance": ["schema validates against audit.v1.json", "events hash-chained"],
      "dependencies": [],
      "provenance": {"intent_span": [36, 52]},
      "swarm": "swarm-alpha",
      "risk": "high"
    },
    {
      "id": "REQ-3",
      "title": "Rate-limit integration tests",
      "description": "Boundary tests at 99/100/101 requests.",
      "acceptance": ["tests green in CI", "no shared state between cases"],
      "dependencies": ["REQ-1"],
      "provenance": {"intent_span": [54, 71]},
      "swarm": "swarm-beta",
      "risk": "low"
    },
    {
      "id": "REQ-4",
      "title": "Health endpoint",
      "description": "/healthz returns 200 with build stamp and uptime.",
      "acceptance": ["responds < 50ms", "includes version"],
      "dependencies": [],
      "provenance": {"intent_span": [73, 90]},
      "swarm": "swarm-beta",
      "risk": "low"
    },
    {
      "id": "REQ-5",
      "title": "Audit query API",
      "description": "Filter audit events by actor and time range.",
      "acceptance": ["pagination stable", "rejects malformed ranges"],
      "dependencies": ["REQ-2"],
      "provenance": {"intent_span": [92, 110]},
      "swarm": "swarm-beta",
      "risk": "medium"
    }
  ],
  "swarms": [
    {"id": "swarm-alpha", "workers": 2, "requirements": ["REQ-1", "REQ-2"]},
    {"id": "swarm-beta", "workers": 2, "requirements": ["REQ-3", "REQ-4", "REQ-5"]}
  ],
  "estimated_parallelism": 2.4,
  "critical_path": ["REQ-2", "REQ-5"]
}
